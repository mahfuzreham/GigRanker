Options -Indexes

<IfModule mod_rewrite.c>
    RewriteEngine On

    # cPanel deployments that keep the Laravel project root in public_html.
    # Serve only the Laravel public directory without exposing application files.
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>
